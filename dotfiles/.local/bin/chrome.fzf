#!/bin/sh
# node:sqlite is still flagged experimental, and the warning node prints for it
# would land in the finder's own window, so the flag lives here.
exec node --disable-warning=ExperimentalWarning \
	"$(dirname "$(readlink -f "$0")")/chrome-fzf.mjs" "$@"
